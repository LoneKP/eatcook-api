# frozen_string_literal: true

require 'rails_helper'

RSpec.describe AuthorizeApiRequest do
  # define :http_auth_header

  # def context "api request has a valid token" do
  #   it { is_expected.to respond_with 200 }
  # end

  # def context "api request has an invalid token" do
  #   it { is_expected.to respond_with 404 }
  # end

  # define :decoded_auth_token

  # it 'returns an instance of a user' do
  #   user = User.new

  #   expect(user.id.to(eq(decoded_auth_token[:user_id])))
  # end
end
